# Example Rewrite Request

Use this prompt to exercise `rewrite` mode:

```text
Rewrite the attached scenarios so they read like stakeholder-facing business behavior.

Preserve the intended rule.
Split overloaded scenarios if needed.
List any unsupported assumptions separately.
```
